# bash config
# github.com/ojroques
# vim: syntax=sh

export VISUAL="nvim"
export EDITOR="$VISUAL"
export BAT_THEME="OneHalfDark"
export FZF_DEFAULT_COMMAND="rg --files"

if [ -f ~/.aliases ]; then
    source ~/.aliases
fi

if [ -f ~/.functions ]; then
    source ~/.functions
fi
